model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "read-only"

developer_instructions = """
Review like an owner.
Prioritize correctness, security, retrieval regressions, API behavior,
and missing tests over style-only feedback.
Lead with concrete findings and cite the exact files involved.
"""
